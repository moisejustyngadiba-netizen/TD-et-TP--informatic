@import url(https://127.0.0.1:5500/index.html)

* {
    font-family: 'Monserrat' ;
}

body  { 
    margin:0 auto 
}

/*tête de la page */
header.header(
       display: flex;
       align-items:conter;

)

header.header im{
    width:150px;
}

section.top-page {
    background: url('../image/bg.jpg');
    background-size:cover;
    height:80vh;
    pading:25px;
}

/*page de bienvenue (landing page) */

.big-title{
    color:orange;
    front-size:80px;
    padding: 20px 50px;
}

/*menu de navigation*/

nav.nav(
    diplay: flex;
    justify-content: space-between;
    list-style:mone
    backgroud-color:red;
    width:400PX;
    margin-left: 30px;
)

nav.nav li{
    color: orange;
}
